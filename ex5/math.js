// a simple math library
console.log('this is math test');
let test = new Array; 
module.exports = {
  add: (a, b) => a + b,  
  sub: (a, b) => a - b,
  constants: {
    pi: 3.14,
    e: 2.71
  },
  fft: {
    add: (a, b) => a + b,
    mul: (a, b) => a * b,
  }
}

